# 手册与配套材料验收

状态：当前交付包括操作手册、知识模板、两个角色提示词、日常 Hook 和 `portal/` 本地应用。不提供统一安装器、Horizon 编排程序或公开 npm 安装包。外部软件按实例记录安装；门户自己的兼容要求和依赖版本以其配置文件为准。

## 当前版本与证据范围（2026-09-20）

| 能力 | 已有验证 | 不能据此宣称 |
|---|---|---|
| 本地 Harness 替代模型 API | Codex / DeepSeek Harness 均完成固定文字与合成资料候选处理；缺失、取消、超时、配置变更及旧 API 拒绝有隔离检查 | 已验证任意资料质量、所有后续版本、完全离线或恶意进程沙箱 |
| 导入与逐条审核保存 | 原件保留、候选对照、具体预览、确认本地写入、恢复和旧知识版本保护 | 模型自行批准、自动修改主题 Wiki 或网页自动 Git 发布 |
| 全页面改版及无刷新导航 | 桌面与窄屏入口、目录交互；菜单和表单保留、历史导航、迟到响应及审核草稿保护 | 未保存输入跨浏览器重载持久化、所有浏览器组合均验证 |
| 回归 | 最近完整检查 45/45，最终导航与审核专项 2/2；结果对应已保存应用版本 `c5408bb` | 本次文档更新重新运行了业务测试或真实模型 |
| IMA / 文件格式 | 单篇资料暂存、同源笔记更新及 Markdown / DOCX / 文字 PDF 既有实例记录 | 所有账号权限、订阅内容、复杂版式或扫描 PDF 都可处理 |
| Git 自动刷新 | 已有知识私库实例完成提交、推送、刷新回执和真实检索核对 | 新克隆 EvoKBase 即自动部署工作流、runner 或 GBrain |

用户已确认本轮功能可用，并要求不再追加业务测试；本次仅修改说明，核对文档链接、示例、编码及其与当前代码的一致性。人工 IDE 端到端审阅、新用户从零部署及长期使用效果仍不能由历史自动检查替代。语雀真实账号验收、扫描 PDF 和钉钉维持暂停。

私有样本、部署地址和原始运行报告保存在用户实例目录，不复制到公共仓库。下文保留早期阶段验收背景；与当前功能不一致的早期描述以本节及[使用与流转](知识库使用与流转.md)为准。

## 本次材料检查

- README 分为快速开始与详细操作手册；详细版保留用户原教程的十章施工、日常操作、最终报告和自身验收结构。
- 主手册保留逐章输入、提示词、产物、验收和恢复要求，补充团队职责、两个子 Agent、原生 Hook 与 Horizon；软件安装由 AI 根据官方说明直接执行，不指定版本。
- Obsidian 新增官方安装、打开同一目录为 Vault、重启与双端读取、首页/链接/模板/附件使用和失败恢复说明。
- Horizon 章节分开说明软件安装与 Codex 评审，不声称安装上游就自动获得 3+1 流程。
- 模板刷新协议使用用户实例记录中的原生命令，不依赖已撤下的 CLI。
- 修改后已核对本地文档链接、UTF-8 无 BOM、私人路径及旧命令残留；旧程序入口已归档。

详细版整理检查：22 个交付文件通过编码检查，24 个本地链接有效，代码块闭合；原十个施工章节及新增小节齐全，原知识库文件 SHA256 前后未变。此次仅验证文档材料，未在本机安装 Obsidian 或重新部署其他软件。

配套处理器可独立验证：

```powershell
node --test tests/hook.test.mjs
```

范围规则同步后 Hook 五项测试全部通过，检查业务/显式查库范围、跳过普通任务、复用查询及评审范围的注入；处理器仍不负责语义分类或启动角色。此检查不安装软件、不调用模型，不能代替真实宿主行为验收。

## 新用户环境验收

| 环节 | 通过条件 |
|---|---|
| 环境与安装 | 仅按手册和官方来源完成；实际依赖、版本、目录与恢复方法有记录 |
| 从零边界 | 新目录初始化，不覆盖已有资料或无关宿主配置 |
| Obsidian | Vault与Agent指向同一新知识目录，重启后读到同一句，链接/模板可用；未自动启用同步、发布或社区插件 |
| 模板 | 五层目录职责清楚，八类模板可用，示例未冒充用户经历 |
| 检索 | MCP 搜索后读取完整正文并核对来源；新会话也可复现 |
| 范围与权限 | 草稿、模板、原始资料等排除项不可检索；写工具被拒绝 |
| 真实调度 | 查询和评审两个原生角色有实际调用结果；无递归或重复推荐 |
| 范围与复用 | 业务分析/开发或明确查库时调用；普通问答、聊天、文档润色、非业务配置维护不启动两个角色；“继续”沿用范围，已有结果可复用 |
| 知识质量 | 重复跳过，有增量才推荐；决策、方案、踩坑与完整案例保留证据 |
| 确认入库 | 展示全文或精确差异后由用户确认，保存前复查目标变化 |
| 团队协作 | 维护者、成员和共享范围明确；候选经有权人员审核；成员从获准版本读取，合并前核对并发变更 |
| 刷新 | 只同步已批准范围；修改和撤回后检索结果一致，失败不假报成功 |
| 反馈进化 | 提案、批准、正式方法更新、后续使用效果可追溯 |
| Horizon（启用时） | 抓取成功/无新增/失败可区分，3+1 真实完成，原文复核，确认后可检索 |
| 定时运行（启用时） | 存在真实触发证据，能暂停和恢复，不自动扩大写入或发送范围 |
| 恢复 | 资料独立可读；必要配置与索引可恢复；其他用户配置保持完整 |

每项记录实际结果、证据路径、未验证内容和失败后的最小处理步骤。未启用的可选能力写“不适用”，未执行的写“未验证”，不能算通过。

## 历史试验与当前状态

旧安装器曾通过 46 项 Node 测试、7 项 Python 测试，并完成过隔离安装、真实抓取和模型评审。这些属于旧程序的试验记录，不作为操作手册从零验收证据。

旧实现、原文档和相关测试已本地归档，退出仓库交付内容，未删除个人知识库或 Horizon 安装。此次完整教程是在保留原知识库文件不变的前提下整理的文档；目前尚未按详细版在新环境完成 Obsidian、整套实装、首次真实入库及长期反馈验证，需要一次独立照文执行。

发布内容限 README、docs、templates、Hook 测试、portal 通用源码及依赖锁、确定后的许可证等必要文件。不得公开整个工作目录、运行报告、凭据或个人实例记录。

## P1 只读网页

可选网页已增加到 `portal/`，启动与聚焦测试见[说明](../portal/README.md)。只读取用户指定的本地展示范围；不调用 GBrain、模型、审核、Git 或刷新脚本。新增测试使用临时合成知识库，不能代替上述整套新用户部署验收。个人实例的浏览器截图、样本和摘要对照保存在仓库外。

## P2 文档关系

沿用 P1 的文件访问与链接解析，提供 D3 力导向图、节点拖拽、画布缩放/平移、悬停关联、一跳图、反向链接、全库视图与筛选。关系保留引用段落/表格行/文件头字段及来源版本；同名、失效、不可读、缺失锚点单列，不从分类名称推断链接。六组聚焦测试覆盖原 P1、图谱行为与布局数据隔离。逐文件读取结果可以重建，无持久图缓存；不作为事务快照、多用户隔离或生产发布验收。图谱视觉参考 Obsidian，但未复制其核心应用代码。

## P3 只读搜索

通过实例配置中的既有无状态 HTTP MCP 读取 `recall` 与 `get_page`，参数、结果顺序和片段可与原接口逐项对照。来源与原文核验后，只为唯一的展示范围内路径生成本地链接；范围外、歧义及核验失败有明确提示。索引原文与本地最新文件分开显示，正文一致不等于文件头或发布提交一致。既有七组测试通过，包含搜索协议、身份冲突、版本差异及失败隔离；收尾新增的第八组聚焦回归通过，覆盖清空查询后刷新及迟到响应隔离。真实查询、UI 与本地文件未变的实例证据保留在仓库外；人工 IDE 端到端审阅和公开发布不由自动测试替代。

## P4 Markdown 导入基础

早期实现单个 UTF-8 Markdown 的独立暂存、原件哈希、资料卡草稿、目标与文件预览、版本绑定及审核任务导出。当前已补充网页中的用户确认本地保存，但网页仍不执行 Git 发布；独立 CLI 按真实确认回执核对 main、既定远程、精确文件与待推送范围，再衔接 P9。失败保留阶段，重复执行不重复提交；后续修改不会被自动回退。

十二组全套隔离检查通过，收尾修改后的五组导入专项全部通过。新增测试使用临时知识库与本地 bare 远端，覆盖同名目录、无关暂存、未推送提交、多个推送地址、字节一致、部分写入/提交后中断恢复、幂等推送、活进程锁保护及刷新回执拒绝条件。本机浏览器验证合成 Markdown 上传、表格预览、未读附件提示、草稿版本变化、刷新恢复、未保存保护、交接下载及窄屏无横向溢出。

2026-09-18补充：一份用户确认的真实 Markdown 已完成私库精确提交、P9 新任务刷新、两个新文件的索引正文/批准摘要核对、真实召回及网页打开。原库无关修改保留在原处，独立干净副本发布后原库快进。Windows 检出换行转换曾导致严格摘要检查拒绝完成；核实仅换行差异且无后续编辑后，仅恢复本任务批准字节，重新验收通过。没有弱化守卫或改写批准内容。

实例原件、报告、回执和私有配置不进入公共仓库；归档不代表上游来源的事实真实性已核验。单份资料验收不代表所有部署或新用户全流程均已通过，该功能仍需人工 IDE 端到端理解和审阅。

P4版本收尾复核补充：修复模板将用户输入解释为JavaScript替换语法、保存/切换的迟到响应覆盖编辑，以及新增同名原件使资料卡裸链接歧义的问题。模板改为原始行单次替换，新资料卡使用显式相对链接；保存期间锁定编辑和任务切换，切换响应再次检查草稿修改。修复前已复现模板和保存保护断言失败，修复后六组导入专项全部通过。已批准资料保持原版本；未改写生产知识或重跑生产发布。
